---
import { Image } from 'astro:assets'

import SocialIcons from './SocialIcons.astro'


const {fullName, blogName, title, institute, profilePicture, profilePicture2, pixelProfilePic} = Astro.props
---
<div class='flex-1'>
   
   <h1 class='text-4xl font-bold mb-4'>
    Hi! I'm <span class='text-secondary'>{fullName}</span>
    </h1>

  <p class='text-lg mb-6'>
    I use this blog as a notebook with shareable links, a space to shape, store and communicate thoughts in a digital format.
  </p>
  
</div>
<div class='size-32 lg:block hidden'>
  
  <Image
    src={pixelProfilePic}
    alt={fullName}
    class='w-full h-full mask mask-circle size-44'
  />
</div>
